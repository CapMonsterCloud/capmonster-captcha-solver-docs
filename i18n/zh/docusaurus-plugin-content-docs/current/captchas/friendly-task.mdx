---
sidebar_position: 27
sidebar_label: Friendly Captcha
title: "Friendly Captcha 自动识别与绕过 | API"
description: "了解如何使用 CapMonster Cloud 自动验证码识别与解决服务的 API 识别 Friendly Captcha！" 
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ParamItem from '@theme/ParamItem';
import MethodItem from '@theme/MethodItem';
import ImageWrap from '@theme/ImageWrap';
import ImagesLayout from '@theme/ImagesLayout';
import MethodDescription from '@theme/MethodDescription'
import PriceBlock from '@theme/PriceBlock';
import PriceBlockWrap from '@theme/PriceBlockWrap';
import { ArticleHead } from '../../../../../src/theme/ArticleHead';

<ArticleHead slug="captchas/friendly-task" />

# Friendly Captcha

<PriceBlockWrap>
  <PriceBlock title="Friendly Captcha" captchaId="friendly"/>
</PriceBlockWrap>

![](./images/friendly-task/friendly.png)

:::warning **注意！**
CapMonster Cloud 默认通过内置代理工作——这些代理已包含在费用内。仅当网站不接受令牌或对内置服务的访问受限时，才需要指定您自己的代理。

如果代理按 IP 授权，请将地址 **65.21.190.34** 加入白名单。
:::

## 请求参数

<div className="bordered-panel">

  <ParamItem title="type" required type="string" />
  **CustomTask**

  ---

  <ParamItem title="class" required type="string" />
  **friendly**

   --- 

  <ParamItem title="websiteURL" required type="string" />
  包含验证码的页面完整 URL。

  ---

  <ParamItem title="websiteKey" required type="string" />
  Friendly Captcha 密钥（*参见[如何查找 sitekey 的值](#如何查找-sitekey-的值)章节*）。<br />
  大致格式如下：`FFMGEMAD2K3JJ35P`
  
  ---

  <ParamItem title="apiGetLib（位于 metadata 中）" required type="string" />
  JS 文件链接。请根据验证码版本指定 JS 文件的 URL：

* **V1：**
  `apiGetLib` = `https://cdn.jsdelivr.net/npm/friendly-challenge@X.Y.Z/widget.module.min.js`，其中 `X.Y.Z` 是 `x-frc-client` 请求头中的客户端版本。

* **V2：**
  `apiGetLib` = 页面加载的 `site.min.js` 文件 URL。示例：`https://cdn.jsdelivr.net/npm/@friendlycaptcha/sdk@X.Y.Z/site.min.js`，其中 X.Y.Z 是客户端版本。


*详情请参阅[创建任务的方法](#创建任务的方法)和[如何确定 Friendly Captcha 版本](#如何确定-friendly-captcha-版本)章节。*

  ---

<ParamItem title="userAgent" type="string" />
浏览器 User-Agent。请使用 CapMonster Cloud 当前支持的值：`userAgentPlaceholder`<br />

可通过以下地址获取最新值：[https://capmonster.cloud/api/useragent/actual](https://capmonster.cloud/api/useragent/actual)。

  ---

<ParamItem title="proxyType" required type="string" />
**http** - 常规 HTTP/HTTPS 代理；<br />
**https** - 当 http 不可用时使用（某些自定义代理必填）；<br />
**socks4** - SOCKS4 代理；<br />
**socks5** - SOCKS5 代理。

  ---

<ParamItem title="proxyAddress" required type="string" />
<p>
代理 IP 地址（IPv4/IPv6）。禁止使用：
- 透明代理
- 本地机器代理
</p>

  ---

<ParamItem title="proxyPort" required type="integer" />
代理端口

---

<ParamItem title="proxyLogin" required type="string" />
代理登录名

---

<ParamItem title="proxyPassword" required type="string" />
代理密码

</div>

## 创建任务方法

请在 `metadata` 中使用与 Friendly Captcha 版本对应的 `apiGetLib` 参数值。

**例如：**

对于 **V1**
```json
  "apiGetLib": "https://cdn.jsdelivr.net/npm/friendly-challenge@0.9.19/widget.module.min.js"
```

对于 **V2**

```json
  "apiGetLib": "https://cdn.jsdelivr.net/npm/@friendlycaptcha/sdk@X.Y.Z/site.min.js"
```
<br />
<Tabs className="full-width-tabs filled-tabs request-tabs" groupId="captcha-type">
  <TabItem value="proxyless" label="CustomTask（无代理）" default className="method-panel">
    <MethodItem>
    ```http
    https://api.capmonster.cloud/createTask
    ```
    </MethodItem>
    <MethodDescription>
      
      **请求示例**
      ```json
      {
        "clientKey": "API_KEY",
        "task": {
          "type": "CustomTask",
          "class": "friendly",
          "websiteKey": "your-website-key",
          "websiteURL": "https://yourwebsite.com/page-with-captcha",
          "userAgent": "userAgentPlaceholder",
          "metadata": {
			"apiGetLib":"https://cdn.example.com/package/widget.module.min.js"
		}
	}
      ```

      **响应示例**
      ```json
      {
        "errorId": 0,
        "taskId": 407533077
      }
      ```
    </MethodDescription>
  </TabItem>

    <TabItem value="proxy" label="CustomTask（使用代理）" className="method-panel">
    <MethodItem>
      ```http
      https://api.capmonster.cloud/createTask
      ```
    </MethodItem>
    <MethodDescription>
      
      **请求示例**
      ```json
      {
        "clientKey": "API_KEY",
        "task": {
          "type": "CustomTask",
          "class": "friendly",
          "websiteKey": "your-website-key",
          "websiteURL": "https://yourwebsite.com/page-with-captcha",
          "userAgent": "userAgentPlaceholder",
          "metadata": {
			"apiGetLib":"https://cdn.example.com/package/widget.module.min.js"
		},
          "proxyType": "your-proxy-type",
          "proxyAddress": "your-proxy-address",
          "proxyPort": 1234,
          "proxyLogin": "your-proxy-login",
          "proxyPassword": "your-proxy-password"
        }
      }
      ```

      **响应示例**
      ```json
      {
        "errorId": 0,
        "taskId": 407533077
      }
      ```
    </MethodDescription>
  </TabItem>
</Tabs>

## 获取任务结果方法

使用 [getTaskResult](../api/methods/get-task-result.mdx) 方法获取 Friendly Captcha 的解决结果。  
响应中的令牌格式取决于网站上使用的验证码版本：

**V1**: `"56a3727f1f9ae4f339c8e512913cd6f8.ac7W...MAKgAAAN+MAQArAAAAjxMBACwAAAB5QgAA.AgAB"`<br />

**V2**: `"AQQA.8Q2TbgK_..._pknXDweJjKT2qwmroOhHcZsU4dHyu-jaGIPx9k7432p_num13buuTu6n4lVA=="`

<div className="method-panel-full">
  <MethodItem>
      ```http
    https://api.capmonster.cloud/getTaskResult
    ```
  </MethodItem>
  <MethodDescription>

  **请求示例**
  ```json
  {
    "clientKey": "API_KEY",
    "taskId": 407533077
  }
  ```

  **响应示例**
```json
{
  "errorId": 0,
  "errorCode": null,
  "errorDescription": null,
  "status": "ready",
  "solution": {
    "data": {
      "token": "56a3727f1f9ae4f339c8e512913cd6f8.ac7W...MAKgAAAN+MAQArAAAAjxMBACwAAAB5QgAA.AgAB"
    }
  }
}
```

  </MethodDescription>
</div>
<br />
获取到的 token 值需要填入对应字段：

| 版本 | token 填充值字段                  |
| -- | ---------------------------- |
| V1 | `input.frc-captcha-solution` |
| V2 | `input.frc-captcha-response` |

## 如何查找 sitekey 值

* 对于 **V1**，可以在网络请求中通过关键词 `sitekey` 或 `puzzle?sitekey=` 进行过滤查找：

![](./images/friendly-task/sitekey.png)

---

* 对于 **V2**，可以在网络请求或页面代码元素中搜索关键词 `sitekey` 或 `data-sitekey`：

![](./images/friendly-task/sitekey1.png)

## 如何识别 Friendly Captcha 版本

Friendly Captcha 主要有两种版本：**V1** 和 **V2**。
可以通过加载的脚本或网络请求来判断版本。

### Friendly Captcha V1

如果页面中存在以下脚本，则使用 V1：

```javascript
widget.min.js
widget.module.min.js
```

或在网络请求中出现 `puzzle?sitekey=`：

![](./images/friendly-task/v1.png)

**要确定客户端版本，需要：**

1. 打开找到的 `puzzle?sitekey=...` 请求，并进入 **Headers → Request Headers**

2. 查找 `x-frc-client` 请求头：

![](./images/friendly-task/v1-1.png)

该请求头包含网站使用的 Friendly Captcha 客户端版本：

```
x-frc-client: <version>
```

**示例值**：*0.9.14, 0.9.19* 等。

3. 确定 `x-frc-client` 版本后，将其填入 CDN 链接：

```
https://cdn.jsdelivr.net/npm/friendly-challenge@<VERSION>/widget.module.min.js
```

例如，如果 `x-frc-client = 0.9.14`，则使用：

```
https://cdn.jsdelivr.net/npm/friendly-challenge@0.9.14/widget.module.min.js
```

该链接需要在创建任务时传入 `apiGetLib` 参数。

---

### Friendly Captcha V2

如果网站加载了 `site.min.js`，则表示使用的是 **Friendly Captcha V2**。

![](./images/friendly-task/v2.png)

在创建任务时，将 `site.min.js` 的链接传入 `apiGetLib` 参数：

![](./images/friendly-task/v2-1.png)

### Friendly Captcha 版本自动识别

您可以在浏览器中使用以下方案来自动化识别 Friendly Captcha 的版本：

<details>
      <summary>显示代码</summary>
```javascript
(async function detectFriendlyCaptcha() {
  const result = {
    version: null,      
    clientVersion: null,
    indicators: [],
    siteMinJsLinks: [],
    detectedCdn: null
  };

  const scripts = Array.from(document.scripts).map(s => s.src);

  for (const src of scripts) {
    if (!src) continue;

    if (src.includes("widget.min.js") || src.includes("widget.module.min.js")) {
      result.version = "V1";
      result.detectedCdn = src;
      result.indicators.push("Found widget script");
    }

    const match = src.match(/friendly-challenge@(\d+\.\d+\.\d+)/);
    if (match) {
      result.clientVersion = match[1];
      result.version = "V1";
      result.detectedCdn = src;
      result.indicators.push("Detected CDN version");
    }
  }

  const puzzleElements = document.querySelectorAll(
    '[id*="friendly"], [class*="frc"], iframe[src*="friendly"]'
  );

  if (puzzleElements.length > 0) {
    result.indicators.push("Found captcha DOM elements");
    if (!result.version) result.version = "V1";
  }

  const resources = performance.getEntriesByType("resource");

  for (const r of resources) {
    if (r.name.includes("puzzle?sitekey")) {
      result.version = "V1";
      result.indicators.push("Found puzzle?sitekey request");
    }
  }

  for (const src of scripts) {
    if (!src) continue;

    if (src.includes("site.min.js")) {
      result.version = "V2";
      result.detectedCdn = src;
      result.indicators.push("Found site.min.js");
      result.siteMinJsLinks.push(src);
    }
  }

  if (!result.version) {
    result.version = "Unknown (not detected)";
  }

  console.log("FriendlyCaptcha Detection Result:");
  console.log(result);

  return result;
})();
```
</details>

## 使用 SDK 库

<Tabs className="full-width-tabs filled-tabs request-tabs" groupId="captcha-type">

  <TabItem value="js" label="JavaScript / TypeScript" default className="method-panel">
  <details>
      <summary>显示代码（浏览器）</summary>
```js
// https://github.com/CapMonsterCloud/capmonster-nodejs-captcha-solver

import {
  CapMonsterCloudClientFactory,
  ClientOptions,
  FriendlyRequest
} from "@zennolab_com/capmonstercloud-client";

document.addEventListener("DOMContentLoaded", async () => {

  const API_KEY = "YOUR_API_KEY"; // 请填写您的 CapMonster Cloud API 密钥

  const client = CapMonsterCloudClientFactory.Create(
    new ClientOptions({ clientKey: API_KEY })
  );

  const friendlyRequest = new FriendlyRequest({
    websiteURL: "https://yourwebsite.com/page-with-friendly-captcha",
    websiteKey: "your-website-key",
    userAgent: "userAgentPlaceholder",
    metadata: {
      apiGetLib: "your-api-get-lib-url",
    },
  });

  // 如有需要，可以检查余额
  const balance = await client.getBalance();
  console.log("Balance:", balance);

  const result = await client.Solve(friendlyRequest);
  console.log("Solution:", result.solution);
});
```
</details>

<details>
      <summary>显示代码（Node.js）</summary>
```JavaScript
// https://github.com/CapMonsterCloud/capmonster-nodejs-captcha-solver

const {
  CapMonsterCloudClientFactory,
  ClientOptions,
  FriendlyRequest,
} = require("@zennolab_com/capmonstercloud-client");

const API_KEY = "YOUR_API_KEY"; // 请填写您的 CapMonster Cloud API 密钥

async function solveFriendlyCaptcha() {
  const client = CapMonsterCloudClientFactory.Create(
    new ClientOptions({ clientKey: API_KEY }),
  );

  const friendlyRequest = new FriendlyRequest({
    websiteURL: "https://yourwebsite.com/page-with-friendly-captcha",
    websiteKey: "your-website-key",
    userAgent: "userAgentPlaceholder",
    metadata: {
      apiGetLib: "your-api-get-lib-url",
    },
  });

  // 如有需要，可以检查余额
  const balance = await client.getBalance();
  console.log("Balance:", balance);

  const result = await client.Solve(friendlyRequest);
  console.log("Solution:", result.solution);
}

solveFriendlyCaptcha().catch(console.error);
```

</details>
  </TabItem>

  <TabItem value="python" label="Python" default className="method-panel">
<details>
      <summary>显示代码</summary>
```python
# https://github.com/CapMonsterCloud/capmonster-python-captcha-solver

import asyncio

from capmonstercloudclient import CapMonsterClient, ClientOptions
from capmonstercloudclient.requests import FriendlyCustomTaskRequest

API_KEY = "YOUR_API_KEY"  # 请填写您的 CapMonster Cloud API 密钥


async def solve_friendly_captcha():
    client = CapMonsterClient(
        options=ClientOptions(api_key=API_KEY)
    )

    friendly_request = FriendlyCustomTaskRequest(
        websiteUrl="https://yourwebsite.com/page-with-friendly-captcha",
        websiteKey="your-website-key",
        userAgent="userAgentPlaceholder",
        metadata={
            "apiGetLib": "your-api-get-lib-url",
        },
    )

    # 如有需要，可以检查余额
    balance = await client.get_balance()
    print("Balance:", balance)

    result = await client.solve_captcha(friendly_request)
    print("Solution:", result)


asyncio.run(solve_friendly_captcha())
```
</details>
  </TabItem>

  <TabItem value="csharp" label="C#" className="method-panel">

<details>
      <summary>显示代码</summary>
```csharp
// https://github.com/CapMonsterCloud/capmonster-dotnet-captcha-solver

using System;
using System.Threading.Tasks;
using Newtonsoft.Json;
using Zennolab.CapMonsterCloud;
using Zennolab.CapMonsterCloud.Requests;

class Program
{
    static async Task Main(string[] args)
    {
        // 请填写您的 CapMonster Cloud API 密钥
        var clientOptions = new ClientOptions
        {
            ClientKey = "YOUR_API_KEY"
        };

        var cmCloudClient = CapMonsterCloudClientFactory.Create(clientOptions);

        // 不使用代理的基本示例
        // CapMonster Cloud 会自动使用自己的代理
        var friendlyRequest = new FriendlyCustomTaskRequest(
            "your-api-get-lib-url"
        )
        {
            WebsiteUrl = "https://yourwebsite.com/page-with-friendly-captcha",
            WebsiteKey = "your-website-key",
            UserAgent = "userAgentPlaceholder"
        };

        // 使用您自己的代理的示例
        // 如果您想使用自己的代理，请取消此代码块的注释
        /*
        friendlyRequest = new FriendlyCustomTaskRequest(
            "your-api-get-lib-url"
        )
        {
            WebsiteUrl = "https://yourwebsite.com/page-with-friendly-captcha",
            WebsiteKey = "your-website-key",
            UserAgent = "userAgentPlaceholder",

            Proxy = new ProxyContainer(
                "123.45.67.89",
                8080,
                ProxyType.Http,
                "username",
                "password"
            )
        };
        */

        // 如有需要，可以检查余额
        var balance = await cmCloudClient.GetBalanceAsync();
        Console.WriteLine("Balance: " + balance);

        var friendlyResult =
            await cmCloudClient.SolveAsync(friendlyRequest);

        Console.WriteLine("Solution:");
        Console.WriteLine(
            JsonConvert.SerializeObject(
                friendlyResult.Solution,
                Formatting.Indented
            )
        );
    }
}
```
</details>

  </TabItem>

</Tabs>
